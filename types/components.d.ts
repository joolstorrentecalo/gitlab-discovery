declare module '@gitlab/ui' {
  import { Component } from 'vue';

  const GlBadge: Component;
  const GlAvatar: Component;
  const GlAvatarLabeled: Component;
  const GlAvatarLink: Component;
  const GlBanner: Component;
  const GlBreadcrumb: Component;
  const GlButton: Component;
  const GlButtonGroup: Component;
  const GlCard: Component;
  const GlDashboardSkeleton: Component;
  const GlDatepicker: Component;
  const GlDaterangePicker: Component;
  const GlDrawer: Component;
  const GlDropdown: Component;
  const GlDropdownDivider: Component;
  const GlDropdownHeader: Component;
  const GlDropdownItem: Component;
  const GlEmptyState: Component;
  const GlFormCheckbox: Component;
  const GlFormCheckboxGroup: Component;
  const GlFormGroup: Component;
  const GlFormInput: Component;
  const GlFormRadio: Component;
  const GlFormRadioGroup: Component;
  const GlFormSelect: Component;
  const GlFormTextarea: Component;
  const GlFriendlyWrap: Component;
  const GlIcon: Component;
  const GlInfiniteScroll: Component;
  const GlIntersperse: Component;
  const GlLabel: Component;
  const GlLink: Component;
  const GlLoadingIcon: Component;
  const GlModal: Component;
  const GlModalDirective: Component;
  const GlNewButton: Component;
  const GlNewDropdown: Component;
  const GlNewDropdownHeader: Component;
  const GlNewDropdownItem: Component;
  const GlNewDropdownText: Component;
  const GlPaginatedList: Component;
  const GlPagination: Component;
  const GlPopover: Component;
  const GlProgressBar: Component;
  const GlResizeObserverDirective: Component;
  const GlSearchBoxByClick: Component;
  const GlSearchBoxByType: Component;
  const GlSegmentedControl: Component;
  const GlSkeletonLoader: Component;
  const GlSkeletonLoading: Component;
  const GlSorting: Component;
  const GlSortingItem: Component;
  const GlSprintf: Component;
  const GlTab: Component;
  const GlTable: Component;
  const GlTabs: Component;
  const GlToast: Component;
  const GlToggle: Component;
  const GlToken: Component;
  const GlTooltip: Component;
  const GlTooltipDirective: Component;

  export {
    GlAvatar,
    GlAvatarLabeled,
    GlAvatarLink,
    GlBadge,
    GlBanner,
    GlBreadcrumb,
    GlButton,
    GlButtonGroup,
    GlCard,
    GlDashboardSkeleton,
    GlDatepicker,
    GlDaterangePicker,
    GlDrawer,
    GlDropdown,
    GlDropdownDivider,
    GlDropdownHeader,
    GlDropdownItem,
    GlEmptyState,
    GlFormCheckbox,
    GlFormCheckboxGroup,
    GlFormGroup,
    GlFormInput,
    GlFormRadio,
    GlFormRadioGroup,
    GlFormSelect,
    GlFormTextarea,
    GlFriendlyWrap,
    GlIcon,
    GlInfiniteScroll,
    GlIntersperse,
    GlLabel,
    GlLink,
    GlLoadingIcon,
    GlModal,
    GlModalDirective,
    GlNewButton,
    GlNewDropdown,
    GlNewDropdownHeader,
    GlNewDropdownItem,
    GlNewDropdownText,
    GlPaginatedList,
    GlPagination,
    GlPopover,
    GlProgressBar,
    GlResizeObserverDirective,
    GlSearchBoxByClick,
    GlSearchBoxByType,
    GlSegmentedControl,
    GlSkeletonLoader,
    GlSkeletonLoading,
    GlSorting,
    GlSortingItem,
    GlSprintf,
    GlTab,
    GlTable,
    GlTabs,
    GlToast,
    GlToggle,
    GlToken,
    GlTooltip,
    GlTooltipDirective,
  };
}
